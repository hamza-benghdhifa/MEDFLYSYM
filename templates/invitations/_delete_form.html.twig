<form method="post" action="{{ path('app_invitations_delete', {'idinvi': invitation.idinvi}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ invitation.idinvi) }}">
    <button class="btn btn-danger btn-block">Delete</button>
</form>
