{% extends 'base.html.twig' %}

{% block title %}Edit Invitations{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit Invitations</h1>

        <div class="row">
            <div class="col-md-6">
                {{ include('invitations/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>

        <div class="row mt-3">
            <div class="col-md-6">
                <a href="{{ path('app_invitations_index') }}" class="btn btn-secondary btn-block">Back to List</a>
            </div>
        </div>

        <div class="row mt-3">
            <div class="col-md-6">
                {{ include('invitations/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
