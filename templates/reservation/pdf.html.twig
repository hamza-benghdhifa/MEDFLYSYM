<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Reclamation</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        h1 {
            font-size: 24px;
            margin-bottom: 20px;
        }
        table {
            border-collapse: collapse;
            width: 100%;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
        }
        th {
            background-color: #f2f2f2;
            font-weight: bold;
            text-align: center;
            vertical-align: middle;
            padding: 12px;
        }
        td {
            text-align: center;
            vertical-align: middle;
            padding: 12px;
        }
        img {
            max-width: 150px;
            max-height: 150px;
        }
        .btn-container {
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .btn {
            padding: 10px 20px;
            margin: 0 5px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            font-weight: bold;
            text-transform: uppercase;
            cursor: pointer;
            color: #fff;
        }
        .btn-primary {
            background-color: #007bff;
        }
        .btn-primary:hover {
            background-color: #0062cc;
        }
        .btn-success {
            background-color: #28a745;
        }
        .btn-success:hover {
            background-color: #218838;
        }
        .btn-danger {
            background-color: #dc3545;
        }
        .btn-danger:hover {
            background-color: #c82333;
        }
    </style>
</head>
<body>
    <center><h1> Donnees Patient</h1></center>
    <table>
        <thead>
            <tr>
                <th scope="col">Nom Patient</th>
                <th scope="col">Nom Medecin</th>
                <th scope="col">date Reservation</th>
               
               
            </tr>
        </thead>
        <tbody>
            {% for articles in articles %}
            <tr>
               
                    <td> {{articles.PatientId.nom  }}</td>
                    <td>{{ articles.medecinId.nom  }}</td>
                    <td>{{ articles.DateRdv|date('Y-m-d H:i:s') }}</td>


           
            </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
