 {{ form_start(reservasionForm,{attr:{novalidate:'novalidate'}}) }}
 



{{ form_widget(reservasionForm) }}


    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>

   
{{ form_end(reservasionForm) }}
