{% extends 'base.html.twig' %}

{% block body %}
    <br>
    <center>
        <h1 style="color: #A0BCF1;">Espase Admin</h1>
    </center>

    <br> <br> <br>
    <br> <br><br> <br>

    {{ form_start(form) }}
    <div class="form-row align-items-end">
        <div class="col">
            {{ form_row(form.nom) }}
        </div>
        <div class="col">
            <div class="form-group">
                <button type="submit" class="btn btn-success">Rechercher</button>
            </div>
        </div>
    </div>
    {{ form_end(form) }}

    <br> <br> <br>
    <br> <br><br> <br>

    <style>
        div {
            text-align: center;
        }
    </style>

    <div>
        <style>
            table {
                width: 100%;
                border: 3px solid #B7CCF4;
            }

            td {
                text-align: center;
                padding: 10px;
            }
        </style>
        <table class="table text-nowrap mb-0 align-middle">
            <thead class="table-secondary">
            <tr>
                <th scope="col" class="col-2">nom Medecin</th>
                <th scope="col" class="col-2">prenom Medecin</th>
                <th scope="col" class="col-2">specialite</th>
                <th scope="col" class="col-2">dateRSV</th>
                
                <th scope="col" class="col-2">Nom PATIENT</th>
                 
            </tr>
            </thead>
            <tbody>
         {% for medecinWithReservation in medecinWithReservations %}
    {% for reservation in medecinWithReservation.reservations %}
        <tr>
            <!-- ... (autres colonnes) ... -->
            <td>{{ medecinWithReservation.medecin.nom }}</td>
            <td>{{ medecinWithReservation.medecin.prenom }}</td>
            <td>{{ medecinWithReservation.medecin.specialite }}</td>
           <td>{{ reservation.DateRdv|date('Y-m-d H:i:s') }}</td>
       <!-- ... (autres colonnes) ... -->
<td>{{ reservation.PatientId is not null ? reservation.PatientId.nom : 'N/A' }}</td>
<!-- ... (autres colonnes) ... -->

            <!-- ... (autres colonnes) ... -->
        </tr>
    {% endfor %}

            {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}
